import React from 'react';
import styles from './Models.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

const Models = () => {
  return <div className={cx('wrapper')}>Models</div>;
};

export default Models;
